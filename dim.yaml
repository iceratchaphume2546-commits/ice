steps:
  # Build Docker image สำหรับ main
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/ice-main:latest', '-f', 'ICE/Dockerfile', 'ICE']

  # Build Docker image สำหรับ dim
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/ice-dim:latest', '-f', 'ICE/Dockerfiledim', 'ICE']

  # Push image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/ice-main:latest']
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/ice-dim:latest']

images:
  - 'gcr.io/$PROJECT_ID/ice-main:latest'
  - 'gcr.io/$PROJECT_ID/ice-dim:latest'

options:
  logging: CLOUD_LOGGING_ONLY

serviceAccount: 'YOUR_SERVICE_ACCOUNT@YOUR_PROJECT_ID.iam.gserviceaccount.com'

