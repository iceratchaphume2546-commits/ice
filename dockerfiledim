# ใช้ Python 3.14 เป็น base image
FROM python:3.14

# ตั้ง working directory
WORKDIR /app

# คัดลอกไฟล์ทั้งหมดลงใน container
COPY . .

# ติดตั้ง dependencies
RUN pip install --no-cache-dir -r requirements.txt

# ตั้งค่าให้ Python output แบบ unbuffered (ช่วยให้ log ขึ้นทันที)
ENV PYTHONUNBUFFERED=1

# รัน main_dim.py แทน main.py
CMD ["python", "main_dim.py"]


